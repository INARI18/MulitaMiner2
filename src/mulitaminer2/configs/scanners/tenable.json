{
  "name": "tenable",
  "source": "TENABLEWAS",
  "record": "tenable",
  "prompt": "tenable_prompt.txt",
  "max_vulns_per_chunk": 3,
  "marker_pattern": "(?:#+\\s+)?(?:\\d+(?:\\.\\d+)*\\s+)?VULNERABILITY\\s+(CRITICAL|HIGH|MEDIUM|LOW|INFO)\\s+PLUGIN\\s+ID\\s+\\d+",
  "marker_ignorecase": true,
  "name_walkback": 1,
  "name_stop_pattern": "^\\s*(Description|Solution|Risk Information|Plugin Details|Reference Information|Identification|HTTP Info|See Also|BID\\b|CWE\\b|WASC\\b|OWASP\\b|CVE\\b).*$|^\\s*https?://",
  "_name_lesson": "The vulnerability NAME is the ONE line immediately BEFORE the VULNERABILITY header (mirror image of the OpenVAS NVT lesson). Verified against the ground truth: names never wrap (max 56 chars). A 2-line walk-back polluted names with the previous block's reference tail ('BID -', 'CVE -') on the JuiceShop report — 22 broken base/instances pairings and 8 dropped blocks. The stop pattern also rejects reference-content lines as a second guard.",
  "pair": {
    "strip_name_suffix": "\\s*Instances\\s*\\(\\d+\\)\\s*$",
    "by": ["plugin"],
    "merge_instances": true,
    "_lesson": "A finding appears as TWO consecutive blocks — Base (Description/Solution/Risk/Plugin Details) and 'Name Instances (N)' (per-URL evidence). Pairing them is structure, not deduplication: it always runs."
  },
  "severity_map": { "INFO": "LOG" }
}
